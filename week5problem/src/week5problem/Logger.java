package week5problem;

// for sake of clerity, the eclipse ide demanded that i name the file "Logger" rather than "logger" and i have no bloddy idea why.
//gave it what it wanted though. cant much argue with a file. 
public interface Logger {
    void log(String message);
    void error(String message);
}